{
  "name": "socrates-monorepo",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w @socrates/backend",
    "dev:mobile": "npm run start -w @socrates/mobile",
    "install-all": "npm install --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  }
}